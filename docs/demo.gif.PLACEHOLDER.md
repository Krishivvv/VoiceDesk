# Demo GIF placeholder

Replace this with `docs/demo.gif` — a short screen recording (8–15 s) of a full
voice round-trip in the Streamlit UI:

1. Click **Record Audio** and ask a question (e.g. "What is your return policy?").
2. Show the transcript card populating (user + agent).
3. Play the spoken MP3 response.

Keep it under ~5 MB so it loads quickly in the README. Once added, the
`![VoiceDesk voice round-trip demo](docs/demo.gif)` image at the top of the
README will render automatically.
